{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".", // 项目根目录作为基础路径
    "paths": {
      "@/*": ["src/*"] // 设置 @ 映射到 src 目录
    },
  },
  "include": [
    "src/**/*.ts",  // 包括 .ts 和 .tsx 文件
    "src/**/*.tsx"
  ]
}
